export default function Archive() {
  return (
    <div>
      I had an old website with some older projects from when I was first
      learning to code. You can see that here... if I ever get to updating the
      link.
    </div>
  );
}
